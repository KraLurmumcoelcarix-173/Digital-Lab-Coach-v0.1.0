"""Layer 3 Mode A benchmark harness (NEVER COMMITTED — local-only).

Same lifecycle as dlc/evaluator (Layer 2): code rides the working tree
during collection, every result lands OUTSIDE the repo, and when the
charts are delivered the whole package moves to the owner's local
projects folder. See README.md and the followbook."""
