"""
Digital Lab Coach web UI: local FastAPI server + Cytoscape.js front end.

The server runs at http://localhost:8765 by default and renders the
parsed .dig as an interactive signal-flow graph. Layer 1 issue overlay
and Layer 2 hover info are layered on top.
"""